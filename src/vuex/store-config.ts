import {
  counter,
  theme,
} from './modules/temp_index';

/**
 * [storeConfig description]
 * solo para pruebas
 * @type {Object}
 */
export const storeConfig = {
  modules: {
    counter,
    theme,
  },
};
