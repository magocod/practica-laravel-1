// TS
import Vue from 'vue';

export default Vue.extend({

  name: 'SideL',

  // variables recibida desde fuera
  props: [
    'list',
  ],

  // almacenar las propiedades
  data() {
    return {

    };
  },

});
